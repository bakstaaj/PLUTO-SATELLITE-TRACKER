# v2.8.4 Receive Diagnostics Panel

Adds a UI-only Receive Diagnostics panel under the hamburger menu.

Validation scope:

- Backend C remains unchanged.
- Web JavaScript syntax validates with Node.
- Web assets deploy to Pluto.
- Served UI assets expose the v2.8.4 receive diagnostics markers.
- Live Pluto `/api/status` and `/api/passes` baseline checks pass.

This is still a placeholder/guidance feature. It does not start receiver hardware and does not add DSP decode support.
